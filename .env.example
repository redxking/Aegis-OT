# Optional immutable-image overrides. The Compose and Dockerfile defaults are already pinned.
# OPA_IMAGE=openpolicyagent/opa:<version>-static@sha256:<verified-multi-platform-digest>
# PYTHON_IMAGE=python:<version>-slim@sha256:<verified-multi-platform-digest>
